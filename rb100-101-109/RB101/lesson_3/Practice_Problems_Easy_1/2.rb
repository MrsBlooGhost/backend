# what is `!=` and where should you use it?
# `!=` returns the negated boolean value of the evaluated result of the expression

# put `!` before something, like `!user_name`
# the opposite of the boolean equivalent of the object referenced by `user_name`

# put `!` after something, like `words.uniq!`
# this is the mutating version of the `uniq` method

# put `?` before something
# this could be the ternary operator

# put `?` after something
# this could be part of a ternary operator

# put `!!` before something, like `!!user_name`
# this is the boolean equivalent of `user_name`